# AI Companion Character Pack 模板

复制 `_template` 目录并改名，然后修改 `character.json`、`persona.md`、`voice.json`、`proactive.json` 和 `memory_seed.json`。

注意：
- 不要写入 API Key、Token、Cookie 或私钥。
- 图片资源放在 `assets/` 目录。
- 不要放入脚本、可执行文件或需要自动运行的命令。
